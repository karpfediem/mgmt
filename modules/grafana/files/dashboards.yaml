apiVersion: 1

providers:
  - name: "{{ .name }}"
    orgId: 1
    folder: "{{ .folder }}"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: {{ .path }}
